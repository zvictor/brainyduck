mutation createUser($username: String!) {
  createUser(data: { username: $username }) {
    _id
  }
}

query allUsers {
  allUsers {
    data {
      username
    }
  }
}

query sayHello($name: String!) {
  sayHello(name: $name)
}
